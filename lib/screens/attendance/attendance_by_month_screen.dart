import 'package:flutter/material.dart';

class AttendanceByMonthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Attendance'),
      ),
    );
  }
}
