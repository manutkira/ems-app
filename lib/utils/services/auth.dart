import 'package:ems/utils/services/base_api.dart';

class AuthenticationService extends BaseService {
  void login(String email, String password) {
    //use this.base_api_url to get base api url.
  }
}
